import { Box, styled } from "@mui/material";

export const StoreRoot = styled(Box)(() => ({
    padding: 24,
}))

export const StoreContentWrapper = styled(Box)(() => ({
    display: 'flex',
    gap: 24,
    margin: '0 auto',
    justifyContent: 'center',
}))
